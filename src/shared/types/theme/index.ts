import { color } from "framer-motion";
import { font } from "./Font";

export const light = {
    color,
    font
}

export {ColorArray, FontArray} from './ThemeArrays';
export type {ColorKeys, FontKeys} from './ThemeKeys';
export type {ColorType, FontType} from './ThemeTypes';